# 聪明钱跟单功能独立使用指南

## 概述

聪明钱跟单功能已独立为单独模块，可以独立启用/禁用，不影响其他跟单策略。

## 快速启用

### 步骤 1: 编辑 .env 文件

```bash
cd ~/poly-mcp
nano .env
```

### 步骤 2: 添加聪明钱配置

```env
# 启用聪明钱跟单（独立模块）
ENABLE_SMART_MONEY=true

# 设置要跟随的聪明钱地址（逗号分隔，不要有空格）
SMART_MONEY_ADDRESSES=0x1234567890abcdef1234567890abcdef12345678,0xabcdef1234567890abcdef1234567890abcdef12

# 聪明钱跟单参数
COPY_TRADE_SIZE_MULTIPLIER=0.1
COPY_TRADE_FETCH_LIMIT=50

# 是否允许真实下单（设置为 true 才会真实下单）
ENABLE_COPY_TRADING_EXECUTION=true
```

### 步骤 3: 保存并重启

```bash
# 保存文件：Ctrl+X, Y, Enter

# 重启应用
pm2 restart poly-mcp-arbitrage

# 查看日志
pm2 logs poly-mcp-arbitrage --lines 50
```

## 配置说明

### 必需配置

1. **ENABLE_SMART_MONEY**: 启用聪明钱跟单
   - `true`: 启用
   - `false`: 禁用（默认）

2. **SMART_MONEY_ADDRESSES**: 要跟随的钱包地址列表
   - 格式：逗号分隔，不要有空格
   - 示例：`0xabc...,0xdef...`
   - 至少设置一个地址才能工作

### 可选配置

- **COPY_TRADE_SIZE_MULTIPLIER**: 跟单比例（0.1 = 10%）
- **COPY_TRADE_FETCH_LIMIT**: 每次拉取的成交记录数（默认 50）
- **ENABLE_COPY_TRADING_EXECUTION**: 是否允许真实下单

## 功能特点

### 独立运行
- 聪明钱跟单是独立模块，不影响其他跟单策略
- 可以单独启用/禁用
- 有独立的统计信息

### 自动去重
- 自动记录已处理的交易ID
- 避免重复跟单同一笔交易
- 自动清理历史记录，防止内存溢出

### 详细日志
- 显示每个地址发现的新交易数量
- 记录跟单信号的详细信息
- 独立的统计信息展示

## 验证功能

重启后，查看日志应该看到：

```
✅ 聪明钱跟单策略已初始化
   监控地址数量: 2
   地址列表: 0x12345678...,0xabcdef12...

🧠 发现 X 个聪明钱跟单信号
📝 聪明钱信号: 市场 XXX, 方向: buy - 跟随地址 0x12345678...
```

如果开启了真实下单：
```
✅ 聪明钱交易执行成功: 市场 XXX, 金额: X USDC
```

## 统计信息

程序会显示聪明钱策略的独立统计：

```
🧠 聪明钱策略统计:
   监控地址: 2
   发现信号: 15
   执行交易: 8
   已处理交易: 150
```

## 与其他策略的关系

聪明钱跟单是**独立模块**，与其他策略互不影响：

- **套利策略**: 独立运行
- **普通跟单策略**: 独立运行（大额交易、高胜率等）
- **聪明钱跟单**: 独立运行（专门跟随指定地址）

可以同时启用多个策略，它们会并行工作。

## 如何找到聪明钱地址

1. **Polymarket 网站**
   - 查看交易排行榜
   - 关注高胜率交易者

2. **链上分析工具**
   - Dune Analytics
   - Etherscan
   - Nansen

3. **交易社区**
   - Discord/Telegram 群组
   - Twitter 上的公开地址

4. **数据分析**
   - 分析历史交易数据
   - 识别高胜率地址

## 安全建议

⚠️ **重要提示**：

1. **先测试信号模式**：
   - 设置 `ENABLE_COPY_TRADING_EXECUTION=false`
   - 观察聪明钱信号是否合理
   - 确认无误后再开启真实下单

2. **选择可靠的地址**：
   - 选择历史胜率高的交易者
   - 避免跟随新地址或可疑地址
   - 定期审查和更新地址列表

3. **设置合理的跟单比例**：
   - 建议从 0.1（10%）开始
   - 不要设置太大，避免风险

4. **监控和调整**：
   - 定期查看统计信息
   - 根据表现调整地址列表
   - 及时移除表现不佳的地址

## 常见问题

### Q: 聪明钱跟单和其他跟单策略有什么区别？

A: 
- **聪明钱跟单**: 专门跟随指定的钱包地址，独立模块
- **普通跟单**: 跟随大额交易、高胜率交易者等，基于市场数据

### Q: 可以同时启用多个策略吗？

A: 可以！它们会并行工作，互不影响。

### Q: 如何添加/移除监控地址？

A: 编辑 `.env` 文件中的 `SMART_MONEY_ADDRESSES`，然后重启程序。

### Q: 跟单信号一直为 0？

A: 检查：
1. `ENABLE_SMART_MONEY` 是否设置为 `true`
2. `SMART_MONEY_ADDRESSES` 是否设置正确
3. 这些地址是否有最近的交易活动

## 完整配置示例

```env
# 基础配置
PRIVATE_KEY=0x你的私钥
POLYMARKET_API_KEY=你的API密钥
POLYMARKET_API_SECRET=你的密钥
POLYMARKET_API_PASSPHRASE=你的密码短语

# 聪明钱跟单配置（独立模块）
ENABLE_SMART_MONEY=true
SMART_MONEY_ADDRESSES=0x123...,0x456...
COPY_TRADE_SIZE_MULTIPLIER=0.1
COPY_TRADE_FETCH_LIMIT=50
ENABLE_COPY_TRADING_EXECUTION=true

# 其他跟单策略（可选，独立运行）
ENABLE_COPY_TRADING=true
ENABLE_COPY_LARGE_TRADES=true
ENABLE_COPY_HIGH_WIN_RATE=false

# 风险管理
MAX_POSITION_SIZE=100
MAX_DAILY_LOSS=1000
MAX_POSITIONS=10
```

## 一键启用命令

```bash
cd ~/poly-mcp && \
echo "" >> .env && \
echo "# 聪明钱跟单配置（独立模块）" >> .env && \
echo "ENABLE_SMART_MONEY=true" >> .env && \
echo "SMART_MONEY_ADDRESSES=0x你的地址1,0x你的地址2" >> .env && \
echo "COPY_TRADE_SIZE_MULTIPLIER=0.1" >> .env && \
echo "ENABLE_COPY_TRADING_EXECUTION=false" >> .env && \
echo "✅ 配置已添加，请编辑 .env 文件设置 SMART_MONEY_ADDRESSES" && \
echo "然后运行: pm2 restart poly-mcp-arbitrage"
```
