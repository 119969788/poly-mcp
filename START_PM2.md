# PM2 启动指南

## 当前状态

PM2 应用不存在，需要重新启动。

## 启动步骤

### 步骤 1: 确保在项目目录

```bash
cd ~/poly-mcp
pwd
# 应该显示: /root/poly-mcp
```

### 步骤 2: 更新代码（获取最新修复）

```bash
git pull
```

### 步骤 3: 验证配置

```bash
# 检查 .env 文件
cat .env | grep PRIVATE_KEY

# 如果已更新代码，可以运行配置检查
npm run check-config
```

### 步骤 4: 启动应用

#### 方法 1: 直接启动（推荐）

```bash
cd ~/poly-mcp
pm2 start src/index.js --name poly-mcp-arbitrage
```

#### 方法 2: 使用配置文件（如果已更新代码）

```bash
cd ~/poly-mcp
git pull  # 确保有 ecosystem.config.js
pm2 start ecosystem.config.js
```

### 步骤 5: 查看状态和日志

```bash
# 查看状态
pm2 status

# 查看日志
pm2 logs poly-mcp-arbitrage

# 查看详细信息
pm2 info poly-mcp-arbitrage
```

### 步骤 6: 设置开机自启

```bash
# 设置开机自启
pm2 startup
# 运行上面显示的命令（需要 root 权限）

# 保存当前进程列表
pm2 save
```

## 完整启动命令（一键）

```bash
cd ~/poly-mcp && \
git pull && \
pm2 start src/index.js --name poly-mcp-arbitrage && \
pm2 logs poly-mcp-arbitrage --lines 20
```

## 验证启动成功

运行后应该看到：

```bash
pm2 status
```

输出应该显示：
```
┌─────┬─────────────────────────┬─────────┬─────────┬──────────┬─────────┐
│ id  │ name                    │ mode    │ ↺       │ status   │ cpu     │
├─────┼─────────────────────────┼─────────┼─────────┼──────────┼─────────┤
│ 0   │ poly-mcp-arbitrage      │ fork    │ 0       │ online   │ 0%      │
└─────┴─────────────────────────┴─────────┴─────────┴──────────┴─────────┘
```

查看日志应该看到：
```
🚀 启动 Polymarket 跟单套利程序...
🔌 连接到 Polymarket...
   钱包地址: 0x...
✅ 已连接到 Polymarket
```

而不是错误信息。

## 如果启动失败

### 检查错误日志

```bash
pm2 logs poly-mcp-arbitrage --err --lines 50
```

### 常见问题

1. **未设置 PRIVATE_KEY**
   ```bash
   # 检查 .env 文件
   cat .env | grep PRIVATE_KEY
   # 如果没有，编辑文件
   nano .env
   ```

2. **Node.js 版本问题**
   ```bash
   node --version  # 应该 >= v16.0.0
   ```

3. **依赖未安装**
   ```bash
   npm install
   ```

## 常用 PM2 命令

```bash
# 启动
pm2 start src/index.js --name poly-mcp-arbitrage

# 停止
pm2 stop poly-mcp-arbitrage

# 重启
pm2 restart poly-mcp-arbitrage

# 删除
pm2 delete poly-mcp-arbitrage

# 查看状态
pm2 status

# 查看日志
pm2 logs poly-mcp-arbitrage

# 查看详细信息
pm2 info poly-mcp-arbitrage

# 监控
pm2 monit
```
